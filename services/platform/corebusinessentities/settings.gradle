pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://repo.spring.io/snapshot' }
        maven { url 'https://repo.spring.io/milestone' }
    }
}

rootProject.name = 'corebusinessentities'

include 'commons'
project(':commons').projectDir = file('../commons')

include 'coreserviceapp'
project(':coreserviceapp').projectDir = file('../coreserviceapp')
